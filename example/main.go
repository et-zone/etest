package main

import (
	"fmt"

	"github.com/et-zone/etest/mock"
)

func main() {

	for i := 0; i < 10; i++ {
		// data := mock.Mock.DefaultDateTime()
		// data := mock.Mock.DefaultTimestamp()
		// data := mock.Mock.DefaultString()
		// data := mock.Mock.Float64(2)
		// data := mock.Mock.Int64()
		// data := mock.Mock.Int()
		fmt.Println(data)
	}

}
